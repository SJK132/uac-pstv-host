name: Host tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  mixer-and-race:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run host tests
        run: make -C tests clean test race
